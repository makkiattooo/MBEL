# MBEL: Полное Справочное Руководство

**Версия:** 1.2.0
**Дата:** Январь 2026

---

## 📖 Содержание

1.  [Введение](#1-введение)
2.  [Язык MBEL](#2-язык-mbel)
    *   [Структура Файла](#21-структура-файла)
    *   [Типы Данных](#22-типы-данных)
    *   [Интерполяция и Переменные](#23-интерполяция-и-переменные)
    *   [Логика и Управление](#24-логика-и-управление)
    *   [Множественное Число](#25-множественное-число)
3.  [Инструменты CLI](#3-инструменты-cli)
4.  [Интеграция Go SDK](#4-интеграция-go-sdk)

---

## 1. Введение

Modern Billed-English Language (MBEL) — это формат локализации нового поколения.

---

## 2. Язык MBEL

### 2.1 Структура Файла

```mbel
@namespace: "features.auth"  # Метаданные

[Экран Входа]                # Секция

title = "Войти"              # Присваивание
```

### 2.2 Типы Данных

*   **Строка**: В двойных кавычках.
*   **Многострочный Текст**: В тройных кавычках `"""`.

### 2.3 Интерполяция и Переменные

Переменные в фигурных скобках `{}`.

*   **Синтаксис**: `Привет, {user_name}!`

### 2.4 Логика и Управление

**Синтаксис:** `ключ(переменная) { варианты }`

#### Точное Совпадение
```mbel
theme(mode) {
    [dark]  => "Тёмная Тема"
    [light] => "Светлая Тема"
}
```

#### Диапазон
```mbel
battery(percent) {
    [0]       => "Разряжен"
    [1-19]    => "Низкий заряд"
    [100]     => "Полный заряд"
}
```

### 3.7 Множественное Число (Сложное!)

MBEL поддерживает сложные правила славянских языков.

**Пример (Русский):**
```mbel
items_ru(n) {
    [one]   => "{n} файл"     # 1, 21, 31...
    [few]   => "{n} файла"    # 2, 3, 4, 22...
    [many]  => "{n} файлов"   # 5, 6, 11, 100...
    [other] => "{n} файла"    # Дробные и пр.
}
```

---

## 4. Инструменты CLI

*   `mbel init`: Мастер установки.
*   `mbel lint`: Проверка синтаксиса.
*   `mbel compile`: Компиляция в JSON.
*   `mbel watch`: Разработка (Hot-reload).
*   **`stats`**: Статистика.
*   **`fmt`**: Форматирование.

---

## 4. Интеграция Go SDK

### 4.1 Архитектура

*   **Manager**: Менеджер переводов.
*   **Runtime**: Среда выполнения.
*   **Repository**: Источник данных.

### 4.2 Инициализация

```go
import "github.com/makkiattooo/MBEL"

func init() {
    mbel.Init("./locales", mbel.Config{
        DefaultLocale: "ru",
        Watch:         true,
    })
}
```

### 4.3 Использование (Функция T)

```go
func HandleRequest(w http.ResponseWriter, r *http.Request) {
    ctx := r.Context()

    // 1. Простой ключ
    title := mbel.T(ctx, "page_title")

    // 2. С переменными
    msg := mbel.T(ctx, "welcome", mbel.Vars{"name": "Дмитрий"})

    // 3. Множественное число
    items := mbel.T(ctx, "cart_items", 5) 
}
```

### 4.4 HTTP Middleware

Автоматический парсинг заголовка `Accept-Language`.

```go
router.Use(mbel.Middleware)
```
